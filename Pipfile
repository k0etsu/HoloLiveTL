[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[[source]]
# Replace the URL below with the specific URL you got from the PyTorch website
url = "https://download.pytorch.org/whl/cu128"
verify_ssl = true
name = "pytorch_cuda"

[packages]

[dev-packages]
torch = {index = "pytorch_cuda"}
torchaudio = {index = "pytorch_cuda"}
transformers = ">=4.30.0"
soundcard = ">=0.4.2"
scipy = ">=1.9.0"
numpy = "==2.2.6"
requests = ">=2.28.0"
tqdm = ">=4.64.0"
huggingface-hub = ">=0.15.0"
pyinstaller = ">=5.0.0"

[requires]
python_version = "3.12"
